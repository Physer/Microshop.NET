name: Indexing - Continuous Integration

on:
  workflow_dispatch:
  push:
    paths:
      - 'backend/indexing/**'
      - '.github/**'

jobs:
  products_continuous_integration:
    uses: Physer/Microshop.NET/.github/workflows/continuous-integration.yml@main
    with:
      service-directory: backend/Indexing